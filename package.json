{
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "unity-build-script-here && npm run copy-manifest",
    "copy-manifest": "mkdir -p Build/.well-known && cp public/.well-known/farcaster.json Build/.well-known/farcaster.json"
  },
  "dependencies": {
    "@farcaster/miniapp-sdk": "^0.2.1",
    "@farcaster/miniapp-wagmi-connector": "^1.1.0",
    "viem": "^2.40.3",
    "wagmi": "^3.0.2"
  }
}
